[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "python-templater"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
exclude = ["venv*", "tests*"]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-v --tb=short"
